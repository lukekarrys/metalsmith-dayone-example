extends base

include mixins/pagination
include mixins/entry-link

block content
  .entries
    h1.content-subhead Entries for #{tag}

    each entry in pagination.files
      +entryLink(entry)

    +pagination(pagination)
